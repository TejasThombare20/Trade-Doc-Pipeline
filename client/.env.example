# Vite dev server config — no secrets needed on the frontend.
# The dev server proxies /api/* to the backend.

# Backend URL (used by Vite proxy, not by browser code)
VITE_API_BASE_URL=http://localhost:8000
